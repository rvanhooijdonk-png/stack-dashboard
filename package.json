{
  "name": "stack-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Read-only statuspagina over de stack: aggregeert bestaande canon, saneert fail-closed, publiceert statische HTML.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:fixture": "node scripts/build.mjs --fixture data/fixture.json --out public",
    "test": "node --test 'test/*.test.mjs'"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
